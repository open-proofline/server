Create `server/docs/code-map.md`.

Explain the backend package layout and request flow in plain English.
Keep it short. Include:
- what each package does
- where incidents are created
- where chunks are uploaded
- where hash verification happens
- where files are stored
- where SQLite metadata is written
- what to review before exposing this beyond localhost/private network