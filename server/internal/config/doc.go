// Package config reads the server's small environment-based configuration.
//
// The backend keeps configuration limited to separate private/public bind
// addresses, local data paths, and upload size limits.
package config
