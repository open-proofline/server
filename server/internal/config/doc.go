// Package config reads the server's small environment-based configuration.
//
// The backend is intentionally private/dev-only in v0.1, so configuration is
// limited to bind address, local data paths, and upload size limits.
package config
