# Historical Prompt: Initial Backend v0.1

This prompt is archived historical/reference-only material.

Do not re-run it against the current project unless you intentionally want to compare history. It predates:

- split private/public listener groups
- media streams
- completed encrypted evidence bundles
- simulator stream completion
- plural bind address support
- current emergency viewer download buttons

Use current reusable prompts in `codex/prompts/` instead.
